export function ChecklistModeNotice() {
  return (
    <p className="rounded-lg bg-secondary px-3 py-2 text-sm leading-6 text-secondary-foreground">
      精简模式只保留必须带、必须确认和爸爸必须执行的项目。可选物品不会删除，可以切换到完整模式查看。
    </p>
  );
}
