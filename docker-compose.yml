services:
  dadkit:
    build:
      context: .
      dockerfile: Dockerfile
    image: dadkit:latest
    ports:
      - "${DADKIT_PORT:-3333}:${DADKIT_PORT:-3333}"
    environment:
      HOSTNAME: 0.0.0.0
      PORT: "${DADKIT_PORT:-3333}"
      NODE_ENV: production
    restart: unless-stopped
